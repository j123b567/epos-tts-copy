##	Process this file with automake run in the top directory to yield Makefile.in

include $(top_srcdir)/Makefile.common
OTHER_CXXFLAGS = -DSOURCEDIR=$(srcdir)

noinst_PROGRAMS = vrfy

vrfy_SOURCES = vrfy.cc

TESTS = vrfy
