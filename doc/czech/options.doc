
Epos je znaènì flexibilní program a bez zásahu do jeho zdrojového kódu
je mo¾né modifikovat jeho chování prakticky v ka¾dém ohledu. To se
dìje pomocí nastavování konfiguraèních parametrù. Ty je mo¾né zadávat
na pøíkazové øádce nebo v konfiguraèních souborech.

Tento soubor je zastaralý jako tento soubor.


Konfiguraèní soubory
--------------------

Není-li øeèeno jinak, v¾dy se zpracovávají soubory fixed.ini, epos.ini
a jeden ze souborù ansi.ini a rtf.ini. Oèekává se, ¾e fixed.ini
obsahuje parametry, které jsou na dané instalaci mìnìny jen výjimeènì, 
zatímco epos.ini obsahuje parametry, které se mìní pomìrnì èasto. Smyslem
tohoto uspoøádání je, aby se èastìji mìnìné parametry nemusely hledat
v záplavì rùzných nezajímavých konstant. Soubory ansi.ini, rtf.ini
ka¾dý obsahují ucelenou konfiguraci výstupu (buï do textu obohaceného
o ANSI escape sequence barev, nebo do formátu RTF) a volí se mezi nimi
parametrem markup_language.

Syntax konfiguraèního souboru je shodná se syntaxí slovníkù popsaných
v doc/dict.doc. Struènì øeèeno, ka¾dý øádek obsahuje dvojici 

jméno_parametru		jeho_hodnota

nebo komentáø. Není-li hodnota uvedena, pou¾ívají se implicitní
hodnoty uvedené v tabulce implicitních hodnot ní¾e.
   
Typy jednotlivých parametrù, jejich identifikátory v textu programu,
jejich struèný popis a pod. lze nalézt v souboru src/options.cc. Tento 
soubor je celkem èitelný i pro neprogramátory. Je té¾ vhodné podívat se
dovnitø do souborù cfg/*.ini. 


Lokální parametry
-----------------


Pøíkazová øádka
---------------

Na pøíkazové øádce je mo¾no uvádìt konfiguraèní parametry pod stejnými
názvy, uvozenými dvojicí pomlèek. Hodnota se od názvu parametru oddìluje
mezerou nebo rovnítkem, lze uvìøit ní¾e uvedeným implicitním hodnotám.

Ekvivalentní pøíklady spou¹tìní Eposu, chci-li vidìt difony od textu
zadaného z klávesnice (poèet mezer nehraje roli):

say --show_diphones     --input_file
say --show_diphones=yes --input_file=
say --input_file        --show_diphones yes

Pouze z pøíkazové øádky lze té¾ vyu¾ívat takzvané negované implicitní
hodnoty. V tomto pøípadì se pøed název parametru pí¹í tøi, nikoli jen
dvì pomlèky a výsledná hodnota je uvedena ve tøetím sloupci tabulky
implicitních hodnot. To má smysl zøejmì jen pro hodnoty typu boolean.
Napøíklad vypnutí tisku difonù z pøíkazové øádky, pokud je zapnuto
v konfiguraèních souborech, se provede ekvivalentnì pomocí

say --show_diphones=false
say --show_diphones no
say ---show_diphones

Mimoto je mo¾no pou¾ívat je¹tì nìkolik jednoznakových parametrù
(short options). Jejich chování se doufejme vypí¹e na "say -h",
respektive "epos -h".


Environment
-----------

Pokud environment obsahuje promìnnou EPOSCFGFILE, pou¾ije se její hodnota
jako hodnota cfg_file místo implicitního epos.ini.


Implicitní hodnoty
------------------

(Tøetí sloupeèek je podle mne k nièemu, a¾ na øádek "pøepínaè".
 Viz "pøíkazová øádka".)

Typ parametru		    Implicitní hodnota		Negace
   èíslo		      0				  0
   pøepínaè		      true (zapnuto)		  false (vypnuto)
   znak			      ¾ádný znak ('\0')		  znak nula ('0')
   øetìzec		      prázdný øetìzec ("")	  øetìzec nula ("0")
   ladìná oblast	      ¾ádná			  ¾ádná
   jednotka (slabika atd.)     (bìhová chyba)		   (bìhová chyba)
   výstupní formát	       (bìhová chyba)		   (bìhová chyba)


Priority
--------

Dojde-li ke konfliktu nastavení na rùzných místech, uplatní se nastavení
v tomto poøadí (klesající priority):

	pøíkazová øádka (dlouhé názvy parametrù)
	pøíkazová øádka (jednopísmenné názvy parametrù)
	environment	(jen pokud jde o cfg_file alias EPOSCFGFILE)
	epos.ini        (pøesnìji, soubor daný parametrem cfg_file)
	ansi.ini        (nebo rtf.ini)
	inventáø	(inv/jazyk/inventáø.ini)
	jazyk		(lng/jazyk/jazyk.ini)
	fixed.ini       (pøesnìji, soubor daný parametrem fixed_ini_file)
	default		(vestavìné hodnoty v src/options.cc)

Pokud se v nìkterém souboru nebo na pøíkazové øádce objeví nìkterý parametr
víckrát s rùznými hodnotami, pou¾ije se poslední z nich.

